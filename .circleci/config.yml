jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: |
          echo $myPageSize
          docker run -t -v $(pwd):/collection node:12-slim /bin/bash /collection/setup.sh
      - store_artifacts:
          path: $(pwd)/reports.html
        

